# SQL-RetailSales-Practice
This spreadsheet was created to analyze trends within a public retail sales dataset and to build experience working with SQL. Each sheet contains a visualization created from data returned by a SQL query, along with a screenshot of the query used to produce the results.
